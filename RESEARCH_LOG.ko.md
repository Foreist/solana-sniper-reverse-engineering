# Solana Sniper 역설계 — 진행 상황과 발견

대회: `solana-sniper-bot-reverse-engineering` · $1,000 (단독 1등) · **마감 2026-08-15 06:00 KST**
타깃 봇: `5brv79eFZ2rGprXNvqgVJBkBptkkw8GJX1XydJyZLyAr`

## 루브릭 (100점)
| 항목 | 배점 |
|---|---|
| Part 1 행동분석 | 20 |
| Part 2 피처 역설계 | 20 |
| Part 2 분류 성능 (PR-AUC, precision) | 15 |
| Part 3 리플리카 + 백테스트 | 20 |
| Part 3 봇과 head-to-head 비교 | 15 |
| 재현성·코드 품질 | 10 |
| 필수요건 (3,000단어 이내 / 커버이미지 / **공개** 노트북 / 레포 링크) | Yes-No |

⚠️ **실격 조건**: `t_decision`(배포 시점) 이후 데이터가 Part 2·3의 피처로 들어가면 **두 파트 0점**.
사후 데이터(`pumpfun_trades`, `mcap_candles`)는 결과 라벨·백테스트 P&L 계산에만 허용.

## 데이터 확보 상태
| 티어 | 파일 | 크기 | 상태 |
|---|---|---|---|
| A 코어 | `half_year_dataset.tar` | 41.5 GB | ⏳ 다운로드 71%+ 진행 중 |
| A 양성 | `bought_deploy_txs*`, `bought_deployers_activity` | 709 MB | ✅ 추출 완료 |
| A 음성 | `not_bought_*` (5.06M 배포, 177M 활동) | ~40 GB | tar 뒷부분, 대기 |
| B 봇 활동 | `5brv79e_activity.parquet` | 12 MB | ✅ 완료 |
| C2 Jito 번들 | 7.8 GB | — | 미착수 (번들 피처용) |
| C3 pumpfun_trades | 16.7 GB | — | **Part 3 필수** |
| C4 mcap_candles | 2.65 GB | — | **Part 3 필수** |
| C1 raw blocks | 429 GB | — | 스킵 (선택, 인블록 위치 정밀도만 향상) |

서버: 코어 `65.21.203.147:48102`, B/C `154.12.118.112:48110~48114`

---

## Part 1 결과 (실측 완료)

### 규모
- 매수 토큰 **16,163개**, 매수 16,192건 / 매도 70,812건 / 소각 2건
- 배포 인덱스와 매칭된 건 15,927
- 기간: **2026-03-12 08:54 ~ 2026-06-30 23:29 UTC** (약 3.5개월. 공칭 1/1 시작이지만 실제 활동은 3월 중순부터)

### 진입
- 사이즈: 중앙값 **$183**, 평균 $263, 표준편차 $233, CV 0.89, p05 $100 / p95 $727
- 지연(배포→첫 매수): **중앙값 0초, ≤0초가 90.7%**, ≤1초 98.5%
  → 제로블록 진입이 지배적. 평균 36.4초는 소수의 늦은 진입이 끌어올린 값

### 청산
- 토큰당 매도 중앙값 **4회**, 부분청산 비율 **96.6%**
- 보유시간 중앙값 **6초** (p25 4초, p75 9초) — 초단기 스캘핑
- 창 종료까지 미청산 토큰 3개뿐

### 손익 — 수수료 회계가 결론을 바꾼다 ⭐
```
GROSS  총 $1,488,270 | 승률 78.8% | 중앙 ROI 13.3%
수수료  가스(우선순위+팁) $439,839 + 거래소 $110,506 = $550,345  (gross의 37.0%)
NET    총   $937,925 | 승률 59.3% | 중앙 ROI  4.7%
```
- 평균 수익 $117.25 / 평균 손실 −$28.16
- Net ROI 십분위: p10 −17.7% / p25 −5.9% / **p50 4.7%** / p75 26.8% / p90 56.4%
- **진입 우선순위 비용 $26.66/건 = 평균 포지션의 10.1%**, 청산 비용은 $0.116/건 (230배 차이)

수수료를 빼먹으면 **승률 19.5pp, 중앙 ROI 약 3배 과대평가**된다. 이게 이번 제출물의
핵심 정직성 논점이고, 루브릭이 Part 3에서 명시적으로 배점하는 항목이다.

### 수수료 구조 (데이터로 검증)
- `gas_native == priority_fee + tip_fee` (상관 1.000, 소수 5자리까지 일치)
- `dex_native/dex_usd`는 별도 거래소 수수료 (매수당 평균 $2.90 ≈ 포지션의 1.1%)
- 둘 다 `cost_usd`에 **포함되지 않음** → cost_usd만으로 계산한 손익은 gross
- 매수 우선순위 지출: 3,067 SOL (priority) + 2,302 SOL (Jito tip) 상당
- SOL 환산가 중앙값 $84.1 (`gas_usd / gas_native`)

---

## 데이터 품질 검증 (피처 설계에 반영됨)

### 조인 키
- **`tx_signer`가 deployer wallet이다** — deploy index의 tx_signer가 activity의 wallet에 100% 존재
- `launch` 이벤트로 교차검증: 매칭된 13,818건에서 `tx_signer == launch wallet`이 **100%**
- **`creator_address`는 채움율 0%** → 사용 불가
- 매수 토큰 중 86.8%만 `launch` 이벤트에 존재 (13.2%는 활동 상한에 잘렸거나 타 플랫폼)

### 활동 데이터 상한의 영향 ⚠️
제공자가 deployer당 **최근 1만건**으로 상한을 뒀다.
- deployer 4,095명 중 **233명(5.7%)이 상한 도달**, 그런데 이들이 **전체 활동의 47.4%** 차지
- 상한군의 최초 기록 중앙값 2026-03-12 vs 비상한군 2026-04-06
- → 활발한 deployer일수록 초기 이력이 잘려서 **`wallet_age_s`가 과소추정**된다
- **대응**: `history_truncated` 플래그를 피처에 추가해 모델이 "측정값"과 "하한값"을 구분하게 함.
  침묵하는 오류를 남기지 않는다.

## 분석 설계에서 잡은 함정 3개

### ① 봇 활동창 밖의 음성은 시간 인공물이다 ⚠️
음성 배포는 **2026-01-01 ~ 06-30**인데 봇은 **03-12부터** 활동했다.
- 음성 5,060,494건 중 창 안은 **3,012,320건(59.5%)**, 창 밖 204만건
- 창 밖을 음성으로 쓰면 모델이 *"1월엔 안 산다"*는 달력 사실을 학습한다 — 선택 규칙이 아니다
- 지표는 좋아지고 실제 성능은 없어진다
- **대응**: `BOT_ACTIVE_FROM = 1773305601`(첫 매수) 이전 배포를 전량 제외.
  불균형 1:318 → **1:189**

### ② 보충자료가 6월 한 달치뿐 → 백테스트 범위 제약
C3(trades)/C4(candles)는 스펙대로 **2026-06만** 커버한다.
- 봇 매수 토큰 16,163개 중 6월분은 **4,189개(26.0%)**
- 월별: 3월 3,361 / 4월 3,466 / 5월 5,110 / 6월 4,226
- **대응**: Part 2 테스트 기간을 분위수가 아니라 **6/1로 고정**.
  그러면 Part 3 백테스트가 홀드아웃 구간과 정확히 일치한다 —
  리플리카는 학습에 쓴 데이터로 거래하지 않고, 백테스트는 없는 데이터를 요구하지 않는다
```
학습: 2026-03-12 ~ 05-31   |   테스트 = 백테스트: 2026-06-01 ~ 06-30
```

### ③ 백테스트의 지배 변수는 "진입 체결가"이고, 범위가 압도적으로 넓다 ⭐
봇의 6월 토큰 4,195개에 고정 보유를 적용했을 때 진입가 정의만 바꿔도 결과가 뒤집힌다.

| 보유 | 진입=배포초 **종가**(비관) | 진입=배포초 **시가**(낙관) |
|---|---|---|
| 3초 | 0.9502 | 1.5775 |
| **6초** | **0.9221** | **1.5043** |
| 10초 | 0.9018 | 1.4481 |
| 30초 | 0.8651 | 1.3219 |

**같은 데이터, 같은 전략인데 −7.8% 손실이거나 +50% 수익이다.** 어느 쪽을 고르든 그건
측정이 아니라 선택이다. 둘 다 단조 감소한다는 점은 공통 — **초기 스파이크 후 가격이 감쇠**하므로
빨리 나오는 것이 중요하다.

**대응 — 유리한 가정을 고르는 대신 알려진 실적을 재현하는 가정을 찾는다.**
체결가를 `entry = open + α·(close − open)`으로 보간하고, **봇의 관측된 6월 중앙 실현배수
1.1153을 재현하는 α를 역산**했다.

```
α:      0.00    0.20    0.40    0.50    0.60    0.80    1.00
배수:  1.5043  1.3099  1.1678  1.1099  1.0586  0.9859  0.9221
                                ↑ 목표 1.1153에 가장 근접
→ α = 0.50
```
물리적으로도 타당하다: 스나이퍼 경쟁에서 첫 체결도, 마지막 체결도 아닌 중간.
**리플리카에 같은 α를 고정 적용**하므로 우리 전략이 기존 봇보다 좋은 체결을 선물받고
이기는 일이 구조적으로 불가능하다. (`ENTRY_FILL_ALPHA = 0.50`, `calibrate_alpha()`)

### ④ 봇의 엣지는 진입 선택이 아니라 청산에 있다
α=1(종가 진입) 기준으로 봇 자신의 토큰을 **수동 보유**하면 전 구간에서 손실이다
(6초 0.9221, 30초 0.8651, 60초 0.8080 — 길게 들수록 악화).
그런데 봇의 실제 실현배수는 **1.1153**이다.
- 봇은 토큰당 매도 중앙 **4회**, 부분청산 **96.6%**
- 즉 "좋은 토큰을 고른다"가 아니라 **먼저 들어가서 뒤이어 오는 매수자들에게 분할 매도**한다
- **대응**: Part 3에서 고정 보유를 베이스라인으로 두고 분할 청산 변형을 따로 비교한다.
  "진입 모델만으로 봇을 이긴다"는 주장은 하지 않는다

## 스키마 메모
`*_deployers_activity.parquet` 32컬럼. 시간 절단(t_decision)에 쓸 키는 `timestamp`(int64 UNIX).
- `event_type`: buy 2.29M / sell 2.13M / launch 411K / burn 43.7K / claim_fee 26.4K / add 7.5K / remove 2.5K
- `launchpad_platform`: Pump.fun 3.93M / pump_agent 354K / pump_mayhem 222K / letsbonk 193K / bags 63K …
- 양성 deployer 4,095명, deployer당 활동 중앙값 76건
- 활동 데이터 시간범위는 2024-01-09까지 소급 → 배포 시점 기준 과거 이력 구축 가능
- 숫자 컬럼이 문자열로 저장돼 있음 → `pd.to_numeric(errors='coerce')` 필수

`bought_deploy_txs_index.parquet`: tx_hash, line_number(1-based), blockTime, blockSlot,
token_address, tx_signer, **creator_address (샘플에서 전부 None — 희소 가능성, 확인 필요)**

봇 활동 데이터에는 **슬롯 번호가 없음** → 슬롯 지연은 초당 2.5슬롯(400ms) 가정으로 추정.
정확한 인블록 위치는 C1(429GB) 없이는 불가. 이 한계를 writeup에 명시할 것.

---

## Part 2 결과 (실측 완료)

피처 테이블 **1,098,455행 × 24피처**. 학습 2026-03-12~05-31 / 테스트 6월.
```
학습: 양성 11,732 + 음성 234,640 (음성은 20:1로 서브샘플, 전체 대비 배율 기록)
테스트: 양성 4,195 + 음성 847,888 (전량 보존) → 실제 불균형 1:202, 유병률 0.492%
```

| 모델 | PR-AUC | ROC-AUC | 리콜 5%의 정밀도 |
|---|---|---|---|
| **GBDT** | **0.0761** | **0.891** | **17.6%** |
| 로지스틱 | 0.0157 | 0.790 | 2.6% |
| 베이스라인(유병률) | 0.00492 | 0.5 | 0.49% |

**PR-AUC가 베이스라인의 15.5배**, 상위 5% 리콜 구간에서 무작위 대비 **36배** 정밀도.

### top-10 피처 (모두 deployer 이력 계열)
`mean_buy_usd` > `total_volume_usd` > `secs_since_last` > `launch_rate_per_day` >
`hist_events` > `sell_to_buy_ratio` > `launches_last_24h` > `hist_launches` >
`hist_sells` > `wallet_age_s`
시간대·요일 같은 약한 피처는 전부 밀려났다.

### ⚠️ 처음에 잡은 누수 (기록 목적)
1차 실행에서 **ROC-AUC가 정확히 0.5000**, 피처 중요도 전부 0.00000이 나왔다.
원인: 부기용 컬럼 `neg_sample_scale`을 피처에 포함시켰다. 학습셋에서 음성=20.0 / 양성=1.0으로
**완벽히 분리**되고 테스트셋에선 전부 1.0이라, 모델이 그것만 학습하고 테스트에서 상수를 뱉었다.
같은 이유로 `deploy_slot`·`deploy_time`도 제외했다 — 시간에 단조증가하므로 시간분할에서는
순수 기간 지표다.

### 누수 재검증 (3종 전부 통과)
1. 학습셋에서 단일 피처로 완벽 분리되는 컬럼: **없음**
2. 학습→테스트 평균이 2배 이상 급변하는 컬럼: **없음**
3. 단일 피처 최대 AUC: **0.769** (`secs_since_last`) — 0.95 임계 아래, 정상

---

## Part 3 결과 (실측 완료) — 0.4초가 전략을 죽인다 ⭐

임계값 상위 0.1% → 853건 진입 후보, 캔들로 결과 해결 852건.

| 슬롯 지연 | 진입 α | 중앙 배수 | 중앙 ROI | Net 승률 | Net 손익 |
|---|---|---|---|---|---|
| **0** | 0.50 | **1.243** | **+6.9%** | 56.0% | **+$29,142** |
| 1 (0.4s) | 0.90 | 1.012 | **−15.4%** | 28.5% | −$334 |
| 2 (0.8s) | 1.00 | 1.000 | −15.4% | 27.1% | −$883 |

수수료가 포지션의 약 11%인데 0.4초 지연으로 배수가 1.243 → 1.012로 무너져 수수료를 못 넘는다.
**이 전략의 전부는 제로블록 체결이고, 실패하면 즉시 손실이다.**

### 봇과의 선택 중첩
```
리플리카 진입 853건 | 봇 매수 16,163건 | 교집합 148건
봇 매수 리콜 0.92% | 리플리카 정밀도 17.4%
```
중첩이 낮다. 정직하게 보고할 사항 — 우리 모델은 봇을 복제한 것이 아니라
"같은 종류의 기회를 다르게 고르는" 전략이다.

### ⚠️ 처음 틀렸던 슬롯 지연 모델 (기록 목적)
1차 구현은 지연을 **무작위 탈락**으로만 처리했고, 그 결과 지연이 늘수록 중앙 ROI가
**올라갔다**(6.9% → 9.6% → 19.3%). 체결 건수가 718 → 302 → 129로 줄면서 생존편향이 생긴 것이다.
지연은 유리할 수 없으므로 이는 명백한 모델 오류였다.
**수정**: 지연은 "더 늦은, 더 나쁜 가격에 체결"로 모델링한다. 캔들이 1초 해상도라 0.4초는
타임스탬프로 표현할 수 없으므로, 같은 초 안에서 시가→종가 경로를 따라 α를 올리는 방식으로
가격에 반영하고, 1초를 넘으면 다음 초로 넘긴다. 이 해상도 한계는 writeup에 명시한다.

---

## 다음 작업
1. tar 다운로드 완료 → 음성 클래스 추출 (5.06M 배포 / 177M 활동)
2. **t_decision 안전 피처 빌더 작성** — 아키텍처로 강제(로드 시점에 `timestamp < t_decision`
   필터 + assert). 심사위원이 "명시적이고 검증 가능한 절단"을 요구함
3. Part 2: 해석 가능 모델(로지스틱/GBDT+SHAP), 시간 기반 분할, PR-AUC 중심 평가
4. C3/C4 받아서 Part 3 백테스트 — **1~2 슬롯 지연 민감도**와 수수료를 반드시 반영
5. 공개 노트북 + GitHub 레포 + 커버 이미지 준비 (필수요건 Yes/No)

## 파일
| 파일 | 역할 | 상태 |
|---|---|---|
| `part1_behavior.py` | Part 1 전체 (규모·진입·청산·손익) | ✅ 실행 검증, 수수료 회계 반영 |
| `features.py` | **t_decision 안전 피처 빌더** (29개 피처) | ✅ 감사 위반 0, 15,927건 1분 내 |
| `part2_model.py` | GBDT+로지스틱, 시간분할, PR-AUC, SHAP top-10 | 작성 완료, 데이터 대기 |
| `part3_backtest.py` | 리플리카 + 수수료·체결확률·슬롯지연 민감도 | 작성 완료, C4 대기 |
| `part1_*.csv` | Part 1 산출물 | ✅ |

### 설계상 실격 방지 장치 (`features.py`)
1. `TruncatedHistory`는 **frozen dataclass**이고 생성 시점에 `timestamp < cutoff`로 물리적 필터
2. 모든 피처 함수는 원본 프레임이 아니라 `TruncatedHistory`만 받음 → 실수로도 미래를 못 봄
3. `audit_truncation()`이 원본을 다시 읽어 위반을 assert → 노트북에 출력해서 심사위원이 확인 가능
4. 예외는 배포 트랜잭션 자체(dev-buy, 수수료, 메타데이터) — 이건 t_decision *시점*이라 허용

### 백테스트 정직성 장치 (`part3_backtest.py`)
- 수수료: 봇 실측치(진입 우선순위 $26.66, 거래소 1.1%, 청산 $0.116)를 매 거래에 부과.
  상수를 하드코딩하지 않고 `measure_bot_costs()`가 데이터에서 재계산
- 체결: 제로블록은 경쟁이므로 우선순위 지불액 대비 체결확률 모델 + **0/1/2 슬롯 지연 3종 실행**
- 청산: 사후 캔들에서 진입+보유시간 시점 가격으로 청산. 기간 최고가 절대 사용 안 함
- 캔들 커버리지 없는 토큰은 무료 승리로 처리하지 않고 **드롭 후 건수 보고**
